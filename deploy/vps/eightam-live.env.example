# Copy to /etc/fvg-scanner/eightam-live.env on the VPS.
# Keep this file private: it contains live-trading credentials.

OANDA_LIVE_TOKEN=replace_with_live_oanda_token
OANDA_LIVE_ACCOUNT_ID=your_live_account_id
OANDA_LIVE_CONFIRM=I_UNDERSTAND_LIVE_50_PERCENT_RISK

NODE_ENV=production
